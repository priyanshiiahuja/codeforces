#include<iostream>
using namespace std;

int main()
{
    string s;
    cin >> s;
    int x = s.size() ;
    for (int i = 1; i <= x; i++){
        if s[i] 
    }
    if (s.size() % 2 == 0) cout << "CHAT WITH HER!" ;
    else cout << "IGNORE HER!";
    return 0;
}