#include<iostream>
using namespace std;

int main()
{
    int n, m, z, count = 0;
    cin >> n >> m >> z; 
    if (n == m) cout << z;
    else if {
        for (int i = 1; i <= z; i++){
            if (n*i == m) count ++; 
        }
    }
    return 0;
}